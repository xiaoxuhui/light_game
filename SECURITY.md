# 安全策略

## 支持范围

安全修复面向 `main` 和最新正式版本。旧版本通常不会单独回溯修复。

## 报告安全问题

请优先使用仓库的 GitHub Private Vulnerability Reporting 功能提交私密报告。若该入口不可用，请通过维护者 GitHub 主页提供的私密联系方式联系；不要先创建公开 Issue。

报告最好包含：

- 受影响版本或提交
- 可复现步骤和最小样例
- 实际影响与攻击前提
- 建议修复或缓解方式（如有）

维护者会尽快确认收到报告，在理解影响和准备修复前避免公开细节。修复发布后会在 CHANGELOG 和 GitHub Release 中致谢；如需匿名请在报告中说明。

本项目没有后端，默认不联网；主要安全边界是存档导入、本地存储、浏览器资源加载、开发与发布供应链，
以及阶段二 APK 的 WebView 外壳与 JS 桥。
