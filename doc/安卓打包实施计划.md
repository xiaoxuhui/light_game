# 安卓打包实施计划

分阶段推进，每阶段一个 commit，便于回滚与 review。
验收标准见 [安卓打包需求与测试用例](./安卓打包需求与测试用例.md)。

技术版本组合**固定不变**（偏离易踩坑）：
AGP 8.5.2 + Gradle 8.7 + JDK 17 + compileSdk/targetSdk 34 + minSdk 24 +
androidx.webkit 1.11.0。

## 阶段 0：准备与形态分类

- [x] 确认网页形态：B 类多文件静态站（`index.html` + `scripts/` + `styles/`）
- [x] 确认无打包器产物，无需改 `base` 路径
- [x] 排查能力清单：`localStorage` 存档、1 处 Blob 导出、1 处 file 导入
- [x] 确认无 HTML5 拖放 / `type="module"` / `fetch` / 外链跳转（四条前置约束全过）
- [x] 基线测试通过（单测 95/95、e2e 60/60）
- [x] 基于 main 切出分支 `feat-android-apk`

> ⚠️ **环境注意**：本机沙箱环境**无法创建含 `/` 的分支名**（`git switch -c feat/android-apk`
> 会静默失败：打印 `Switched to a new branch`，但 `.git/refs/heads/feat/` 不生成）。
> 因此分支名改用 `feat-android-apk`。切分支后必须用 `git branch --show-current` 复核。

## 阶段 1：工程骨架

- [ ] 复制 `android/` 模板（app 模块、gradle wrapper、tools、README）
- [ ] `settings.gradle.kts`：`rootProject.name = "LightGame"`（ASCII）
- [ ] `app/build.gradle.kts`：namespace/applicationId = `com.xiaoxuhui.light`，
      versionCode 1，versionName 1.1.0
- [ ] `MainActivity.kt` 迁移到 `com/xiaoxuhui/light/`，改 package、`ASSET_FILE = index.html`、
      桥名 `LightAndroid`、导出兜底名
- [ ] 深色主题适配：背景色 `#08090C`（网页 `--bg-deep`），
      `themes.xml` / `values-v27/themes.xml` 改用 `Theme.Material.NoActionBar`，
      `windowLightStatusBar=false`
- [ ] `strings.xml` 应用名「光的游戏」；`colors.xml` 加 `app_accent #FFD479`
- [ ] `AndroidManifest.xml` 主题引用改为 `@style/Theme.LightGame`

## 阶段 2：应用图标

- [ ] ImageGen 生成两张 1024 原图 → `android/icon-source/`
      （`icon-full.png` 深色背景 + 光路；`icon-foreground.png` 前景）
- [ ] 实测标定去水印裁剪框（`FULL_CROP` / `FORE_CROP`），调 `android/tools/make-icons.py`
- [ ] 生成各密度 PNG：mipmap-mdpi…xxxhdpi 的传统图标 + 自适应前景
- [ ] 取样图标背景色更新 `ic_launcher_background.xml` 渐变

## 阶段 3：资源同步与结构测试

- [ ] 复制 `scripts/sync-android-assets.mjs` + `android-assets.config.mjs`（B 类配置）
- [ ] `package.json` 增加 `sync:android` / `check:android`
- [ ] 复制 `tests/android-shell.test.js`，改 `EXPECT` 常量与包路径
- [ ] `.gitattributes` 增加 `android/gradlew text eol=lf` 与 jar/apk 二进制规则
- [ ] `storage.js` 的 `downloadFile()` 接 JS 桥 + `tests/storage.test.js` 补桥分支断言
- [ ] 首次同步并核对文件数/字节数

## 阶段 4：CI

- [ ] 复制 `.github/workflows/android-apk.yml`
- [ ] `paths` 改为 `index.html` / `scripts/**` / `styles/**`
- [ ] `branches` 增加无斜杠形式 `feat-*`（配合阶段 0 的分支名限制）
- [ ] U03 步骤改为校验 `assets/index.html` 与仓库 `index.html` 一致
- [ ] 产物名改为 `light-game-debug-apk`，Release 文案与文件名改为 light_game

## 阶段 5：出包

- [ ] 推送 `feat-android-apk`，触发 CI（首次约 5–8 分钟）
- [ ] 用 `curl` 打 GitHub API 读真实 `status/conclusion`（**勿信 WebFetch 缓存**）
- [ ] 失败时读构建日志定位 Kotlin/Gradle 报错（workflow 已把 `^e:` 写成 `::error`）
- [ ] 校验产物：APK 可下载、体积合理、sha256 记录

## 阶段 6：合并与发布

- [ ] 合并回 main：`git push origin feat-android-apk:main`
- [ ] 打 tag `v1.1.0` 并推送，workflow 自动把 APK 挂到 Release
- [ ] Release 文案说明安装方式与已知取舍

## 阶段 7：真机验证

- [ ] 用户安装到手机/平板，按 U05–U20 逐项验证
- [ ] 反馈后补测试报告文档

## 风险与已知取舍

| 项 | 说明 |
|---|---|
| 导出交互差异 | WebView 不支持 Blob 下载，改由 JS 桥写系统下载目录，提示语为「已保存到…」 |
| 无物理键盘 | 手机端不提供键盘操作，但所有操作都有触摸等价路径（工具栏点击 + 网格点击） |
| assets 含开发脚本 | 同步整个 `scripts/` 目录，会把 `serve-static.js` 一并带入（约 3 KB，无功能影响） |
| 分支名限制 | 沙箱环境不支持含 `/` 的分支名，统一用 `feat-android-apk` |
| `file://` 与 https 域差异 | WebView 走 `appassets.androidplatform.net` 的 https 域，`localStorage` 的 origin 与 `file://` 不同；安卓端存档从零开始，属预期 |
