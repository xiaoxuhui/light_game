# 光的游戏：需求与测试用例

## 文档状态

| 项目 | 内容 |
|---|---|
| 版本 | v0.1.0（工程骨架） |
| 状态 | **玩法需求待定稿** —— 本文件先锁定工程约束与待确认清单 |
| 实施策略 | 先 Web 版（纯静态、零运行时依赖），跑通后再打包安卓 APK |
| 更新方式 | 玩法定稿后，把「二、待确认决策」逐条改写为正式需求条目 |

## 一、产品需求

### 1. 产品定位

- 名称：光的游戏（仓库 `light_game`）。
- 形态：单页浏览器小游戏，电脑端优先（鼠标 + 键盘），同时兼容触摸设备。
- 完全离线：不联网、无后端、无账号、无统计上报。
- 零运行时依赖：不使用打包器、不引 npm 运行时包、不引 CDN。

### 2. 交付形态

| 阶段 | 交付物 | 验收方式 |
|---|---|---|
| 一 | Web 版（静态站点） | `npm run serve` 与 `file://` 双通道可用 |
| 二 | 安卓 APK | 真机安装，断网可玩 |

### 3. 工程约束（为阶段二预留，不可违反）

这些约束存在的唯一理由是让阶段二打包 APK 不返工：

- 资源引用一律相对路径。绝对路径 `/xxx` 在安卓 WebView 里会解析到错误域名并 404 白屏。
- 不使用 ES Module（`type="module"`）。`file://` 下会被 CORS 拦截；脚本按 `<script defer>` 顺序加载。
- 交互一律用 Pointer Events。HTML5 拖放（`dragover` / `drop`）在安卓触摸端不触发。
- 文件导出必须收敛到单一函数 `downloadFile(name, blob)`。WebView 不支持 `Blob` + `<a download>`，
  安卓阶段要整体替换为 JS 桥调用；调用点收敛好了，届时只改一个函数。
- 随机性必须可注入种子，否则单测无法复现。
- 版本号在 `package.json`、页面展示与 CHANGELOG 三处保持一致（由 `tests/static-app.test.js` 断言）。

### 4. 运行时环境要求

- 现代浏览器：支持 Canvas 2D、Pointer Events、`ResizeObserver`、`localStorage`。
- Node.js 20.19+（仅开发与 CI 需要）。
- 目标视口：360×640 竖屏（手机）到 1280×800 横屏（桌面），含刘海安全区。

## 二、待确认决策

玩法核心尚未确定。以下问题定稿后改写为正式需求条目与 U 编号用例，
技术方案由 `设计文档.md` 承接。

### 2.1 玩法核心

| 编号 | 问题 | 影响面 |
|---|---|---|
| Q01 | 玩家操作的到底是什么？发射光线、操纵棱镜/反射镜，还是管理光源与光路？ | 全部模块 |
| Q02 | 关卡制（固定解法）还是沙盒制（自由创造）？ | 内容组织、存档格式 |
| Q03 | 光的行为模型到哪一档：仅直线传播 + 镜面反射，还是含折射、色散、衍射、半透镜、光强调减、滤色？ | 引擎复杂度与性能上限 |
| Q04 | 是否存在光源间的叠加／干涉（同色相加变亮、异色混合）？ | 计算模型（线性叠加 vs 光路追踪） |
| Q05 | 是否有时间维度（脉冲、移动光源、逐帧演化）？ | 渲染循环与状态模型 |

### 2.2 界面与交互

| 编号 | 问题 | 影响面 |
|---|---|---|
| Q06 | 网格制（回合／离散）还是连续空间（物理模拟）？ | 坐标系统、碰撞与吸附 |
| Q07 | 需要哪些操作：放置、旋转、拖动、删除、撤销／重做、清空？ | 输入层与历史栈 |
| Q08 | HUD 展示什么：关卡编号、步数、光源数、用时、提示？ | 布局与状态栏 |
| Q09 | 是否需要关卡列表／关卡编辑器／存档？ | 存档格式与导入导出 |

### 2.3 内容

| 编号 | 问题 | 影响面 |
|---|---|---|
| Q10 | 初始关卡数量与难度曲线？ | 内容工作量 |
| Q11 | 是否内置编辑器，允许自制关卡并导出 JSON 分享？ | 导入校验（必须完整校验，不信任文件内容） |

### 2.4 非功能

| 编号 | 问题 | 影响面 |
|---|---|---|
| Q12 | 性能上限：最大元件数、光路求解网格规模、可接受的单帧耗时？ | 引擎数据结构选型 |
| Q13 | 是否需要中文以外的语言？ | 文案组织方式 |
| Q14 | 是否要求键盘全操作可达（无障碍）？ | 输入层设计 |

## 三、单元测试用例

单元测试针对**纯计算层**，不依赖浏览器。骨架阶段已落地 11 条结构断言，
玩法实现后按下表继续追加（编号规则：`tests/<模块>.test.js` ↔ 本表条目）。

| 编号 | 用例 | 状态 |
|---|---|---|
| T01 | 页面版本号与 `package.json`、CHANGELOG 一致 | ✅ 已实现 |
| T02 | 入口页资源引用全部存在且为相对路径 | ✅ 已实现 |
| T03 | 脚本按 `defer` 加载且不使用 ES Module | ✅ 已实现 |
| T04 | 工程门禁文件齐全（LICENSE / CI / eslint / gitignore 等） | ✅ 已实现 |
| T05 | 本地服务端口与 Playwright 配置一致，且避开 conway-life-game 的 4173 | ✅ 已实现 |
| T06 | 光引擎纯函数（传播、反射、折射等） | ⏳ 待玩法定稿 |
| T07 | 存档序列化／反序列化往返一致，旧格式兼容 | ⏳ 待玩法定稿 |
| T08 | 导入文件的非法输入拒绝路径 | ⏳ 待玩法定稿 |

## 四、交互测试用例

交互测试走 Playwright，真实浏览器执行，桌面与移动两个视口都要跑。

| 编号 | 用例 | 状态 |
|---|---|---|
| U01 | 打开页面即进入可玩状态，无需任何网络请求 | ⏳ 待玩法定稿 |
| U02 | 鼠标与触摸两种输入都能完成「放置元件 → 调整方向 → 触发计算」全流程 | ⏳ 待玩法定稿 |
| U03 | 断网后刷新，玩法与联网时完全一致 | ⏳ 待玩法定稿 |
| U04 | 页面加载无控制台报错，画布按 DPR 初始化并已绘制内容 | ✅ 已实现 |
| U05 | 窗口尺寸变化后画布跟随重算，不出现空白或错位 | ✅ 已实现 |
| U06 | 页面切到后台再切回，渲染循环正确暂停与恢复 | ✅ 已实现（`__lightGame` 手柄） |
| U07 | 撤销／重做后状态与操作序列一致 | ⏳ 待玩法定稿 |
| U08 | 长期运行不出现状态重复或数据损坏 | ⏳ 待玩法定稿 |
| U09 | 手机竖屏与平板横屏都不被刘海／系统栏遮挡 | ⏳ 待玩法定稿 |

## 五、后续讨论清单

- [ ] 玩法核心 Q01–Q05 定稿
- [ ] 界面交互 Q06–Q09 定稿
- [ ] 内容规模 Q10–Q11 定稿
- [ ] 非功能指标 Q12–Q14 定稿
- [ ] 定稿后同步更新 `设计文档.md` 的模块分层与 `实施计划.md` 的阶段拆分

## 六、已确认的产品决策

| 日期 | 决策 | 理由 |
|---|---|---|
| 2026-09-19 | 仓库名用下划线 `light_game`（非连字符） | 肖工指定 |
| 2026-09-19 | 仓库 public | 与现有三个仓库一致，可直接开 GitHub Pages |
| 2026-09-19 | 不提前铺安卓骨架，等 Web 版跑通再做 | 打包方案的前提是网页已能正常运行 |
| 2026-09-19 | 文档目录用 `doc/` + 中文文件名 | 对齐 EML、turing-machine-simulator |
| 2026-09-19 | 本地服务端口用 4174 | 4173 已被 conway-life-game 占用，同时启动会连错站点 |
