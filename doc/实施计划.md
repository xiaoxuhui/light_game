# 光的游戏：实施计划

## 目标

按「需求 → 设计 → 计划 → 实现 → 验证 → 发布」的流程，交付一个可离线运行的浏览器小游戏，
并在 Web 版稳定后打包成安卓 APK。

需求对标 Steam《RAX》，第一版范围锁定为**光路核心 + 关卡模式**（详见 `需求与测试用例.md`）。

## 执行原则

- **规划先行**：动手写代码前先更新 `需求与测试用例.md` 与 `设计文档.md`。
- **每功能一个 commit**：前缀规范 `feat` / `fix` / `refactor` / `docs` / `test` / `ci` / `chore`。
  底层／接口先，上层／页面后；新模块先，集成后；文档独立。
- **测试驱动**：每个阶段的测试全绿才进下一阶段；前端改动必须做真实浏览器交互验证。
- **不为了让测试通过而编造规则**：产品规则不确定时停下来问，不猜。
- **小步快走**：每个阶段是一个可独立验收的闭环，完成后单独提交。
- **不扩大改动范围**：跑偏的发现记进本文件的待办，不顺手混进当前提交。

## 阶段 0 · 工程骨架（v0.1.0）✅

- [x] 目录结构与单页入口（`index.html` / `styles/main.css` / `scripts/app.js`）
- [x] 零依赖本地静态服务，端口 4174（避开 conway-life-game 的 4173）
- [x] 画布引导链路：DPR 缩放、`ResizeObserver` 跟随、隐藏暂停、动效偏好
- [x] 待机光晕占位渲染
- [x] ESLint 扁平配置 + `node --test` 结构断言（11 条）+ Playwright 冒烟（7 条，桌面 + 移动）
- [x] GitHub Actions CI（Node 20.19 / 22.13 / 24 + 浏览器冒烟）
- [x] MIT License、README、CHANGELOG、CONTRIBUTING、CODE_OF_CONDUCT、SECURITY
- [x] `doc/` 三份规划文档
- [x] `.gitattributes` 换行规范（为安卓 `gradlew` 预留）
- [x] `npm test` 全绿（11 单测 + 12 浏览器冒烟）
- [x] 建立 GitHub 仓库并推送 `main`（`xiaoxuhui/light_game`，public）
- [x] 浏览器冒烟本地跑通 + CI 两轮全绿

## 阶段 1 · 需求与设计定稿 ✅

- [x] 玩法核心 Q01–Q05 定稿（对标 RAX）
- [x] 界面交互 Q06–Q09 定稿
- [x] 内容规模 Q10–Q11 定稿
- [x] 非功能指标 Q12–Q14 定稿（Q12 数值待原型测量后回填）
- [x] 冻结技术选型：正交俯视网格、8 方向传播、RGB 掩码颜色模型
- [x] 补全 T06–T16 / U01–U11 用例编号

## 阶段 2 · 玩法实现

按依赖顺序拆分，**每项一个 commit**，测试先于实现。

### 2.1 光路核心（纯计算）

- [ ] `scripts/light-core.js`：方向表、颜色掩码、反射公式 `(−dy,−dx)` / `(dy,dx)`、元件规则表
- [ ] `tests/light-core.test.js`：T06 轴向反射 8 组合 / T07 斜向边界 / T08 分光 / T09 二向色 / T10 颜色合成

### 2.2 求解器

- [ ] `scripts/light-engine.js`：工作队列追踪、`visited` 剪枝、4096 光段上限、目标判定、星级计算
- [ ] `tests/light-engine.test.js`：T11 终止与剪枝 / T12 幂等 / T13 星级四档

### 2.3 教学关与首个可玩闭环

- [ ] `scripts/levels.js`：schema 校验函数 + 第 0 章 4 关（依次引入直射、反射镜、分光镜、二向色镜）
- [ ] `tests/levels.test.js`：T14 全部关卡自检
- [ ] `scripts/renderer.js`：网格、元件图形、光段折线、目标状态（**替换掉待机占位渲染**）
- [ ] 输入层：Pointer Events 点选 / 放置 / 旋转 / 删除
- [ ] HUD：顶栏、工具栏（含配额）、通关结算浮层
- [ ] 关卡切换与星级记录（内存态）
- [ ] **验收点**：4 个教学关可在桌面与移动视口完整通关

### 2.4 编辑能力完善

- [ ] 撤销／重做历史栈（快照式）
- [ ] 清空本关（二次确认）
- [ ] 键盘全操作可达（数字键、方向键、`R`、`Delete`、`Ctrl+Z`、`Esc`）

### 2.5 正片关卡

- [ ] 第 1 章 6 关（颜色拆分与合成入门）
- [ ] 第 2 章 6 关（分光镜 + 多目标 + 元件数约束下的最优解）
- [ ] 每关人工验证 `par` 可达且不小于实际最小值

### 2.6 存档与导入导出

- [ ] `scripts/storage.js`：`localStorage` 读写、降级到内存存档、版本号与迁移位
- [ ] 自动存档（元件增删改后写入）与历史最好星数
- [ ] 具名工作台快照（对应 RAX「存一下」的能力）
- [ ] `downloadFile()` 收敛 + JSON 导出
- [ ] 导入 + 七项完整校验 + 明确错误提示
- [ ] `tests/storage.test.js`：T15 往返 / T16 拒绝路径

### 2.7 e2e 补齐

- [ ] U01–U03、U07–U11 全部落地，桌面与 Pixel 7 两个视口都跑

## 阶段 3 · Web 版功能完善

- [ ] 响应式打磨：360×640 到 1280×800、刘海安全区、工具栏不遮挡网格
- [ ] 无障碍：键盘可达、`prefers-reduced-motion` 覆盖新动画
- [ ] 性能：测量并回填 Q12 指标（最大元件数、网格规模、单帧耗时），必要时优化求解器
- [ ] `overflow` 提示与极端关卡的降级表现

## 阶段 4 · Web 版发版

- [ ] CHANGELOG 落版本号与日期，README 去「骨架」措辞、补截图与在线演示链接
- [ ] 三处版本号一致（`package.json` / 页面 / CHANGELOG）
- [ ] fast-forward 合并回 `main`，打 tag `vX.Y.Z`
- [ ] 开启 GitHub Pages（source = main /）
- [ ] 建 GitHub Release

## 阶段 5 · 安卓 APK

复用 `web-app-to-android-apk` skill。

- [ ] 第 0 步形态判定：本项目为「B 多文件静态站」，同步 `index.html` + `scripts/` + `styles/`
- [ ] 分支 `feat-android-apk`（**无斜杠**，沙箱环境建不出含 `/` 的分支）
- [ ] 复制工程模板到 `android/`，逐项替换包名／应用名／主题色／入口文件名
- [ ] 包名 `com.xiaoxuhui.lightgame`，应用显示名「光的游戏」，minSdk 24
- [ ] ImageGen 出图标（`icon-full.png` + `icon-foreground.png`），跑 `make-icons.py`
- [ ] `sync:android` / `check:android` 脚本 + 结构断言并进 `npm test`
- [ ] 导出改走 JS 桥 `LightGameAndroid.saveFile`
- [ ] `.github/workflows/android-apk.yml`，tag 触发挂 Release
- [ ] 真机安装验证（断网可玩、旋转不丢状态、返回键行为、刘海不遮挡）

## 第二版及以后（不在当前范围）

- 时序机制：延迟镜、冷热镜、单通镜、条件触发、状态保持（需引入时间步模型）
- 自由实验室（Free Lab）
- 蓝图 / 芯片封装与分享
- 音效与成就体系

## 待办（不混入当前提交）

- [ ] Q07 细节、Q08 HUD 完整清单、Q12 性能数值，分别在对应阶段回填 `需求与测试用例.md`
- [ ] 本机 `gh` CLI 未安装，建仓走 GitHub API；若后续频繁操作可考虑装 `gh`
- [ ] 沙箱环境创建不了含 `/` 的分支名，统一用 `feat-xxx`；切完必须复核
      `git branch --show-current` + `git log --oneline -1`
- [ ] 长命令（`npm test`、`cp -r` 大目录）用后台任务，避免 120s 超时中断导致状态不一致
- [ ] 同一文件的多次编辑必须串行
- [ ] `git status -sb` 出现 `[origin/xxx: gone]` 未必是真的远程删除，
      用 `git ls-remote --heads origin` 核实
