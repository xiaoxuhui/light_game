# 变更日志

本文件记录重要的用户可见变化。版本号遵循语义化版本；发布日期在正式发布时填写。

## [1.2.0] - 2026-09-19

元件改版：移除三种二向色镜、新增三态棱镜，全 16 关的 `par` 用穷举重新确认。

### Added

- **棱镜（`prism`）**：把入射光按颜色分量拆成三束 —— 红色朝入射方向的左转 90°、绿色直行、
  蓝色右转 90°。三态朝向决定「哪个颜色走直行」，点击已放置的棱镜或按 `R` 循环切换。
  缺哪个分量就不出哪一束：黄光只出两束，纯红光只出一束。
- **`npm run verify:par`**：新工具 `tools/par-search.mjs`，逐层加深穷举出每关的**最少元件数**。
  参考解只能证明局部最优，这个工具证明的是全局最优。16 关全部与声明的 `par` 一致。
- 浏览器验收新增棱镜三态轮转用例；单测新增棱镜规则、槽位表与两条结构性不变量断言。
- **安卓 APK 重新发布**：`light-game-android-1.2.0-debug.apk`，`versionName` `1.2.0` /
  `versionCode` `2`，网页版与安卓版版本线重新对齐（1.1.0 时曾一度分开演进）。

### Fixed

- **修复「装不上新版本」**：以往没有配 `signingConfig`，AGP 会在每台构建机上自动生成
  **随机** debug keystore；GitHub Actions 每次都是全新 runner，于是每次发布的 APK 签名都不同，
  老用户覆盖安装新包会被系统直接拒绝（`INSTALL_FAILED_UPDATE_INCOMPATIBLE`）——
  现象就是「明明有新版本，却一直更新不了」。
  现在把 `android/app/debug.keystore` 随仓库提交，并在 `build.gradle.kts` 里显式引用，
  签名从此稳定可覆盖升级。debug key 的密码本就是公开的（`android` / `androiddebugkey`），
  没有保密价值，唯一重要的是它保持不变。
  ⚠️ **从 1.1.0 升级到 1.2.0 的用户需要先卸载旧版**（旧包用的是当年那次构建的随机签名，
  无法用新签名覆盖），之后再升级就能直接覆盖安装了。卸载会清空本地存档。

### Changed

- **移除 `dichroicR` / `dichroicG` / `dichroicB`**。二向色镜是「指定色走一路、其余颜色一起走
  另一路」的**二分**元件，棱镜是**三分**元件，两者语义不同 —— 功能被覆盖后一并删除，
  代码里不留半成品分支。依赖二向色镜的关卡提示与配额同步改写。
- **颜色章（`c01`–`c06`）不再发放分光镜**，只发棱镜与反射镜。反射镜无法分束，所以
  「一束光喂饱多个目标」在这个章节**只能靠棱镜**完成；5 关的 `par` 随之从 1/2/2/2/2
  调整为 3/3/4/4/4。布局与目标位置一个没动。
- 教学第 4 关 `t04` 改为棱镜教学（原本教的是红二向色镜）。
- `c05` 更名为「品红与绿」：原名「绿镜的名堂」引用了已移除的元件。
- 棱镜的三颗色点按**实际入射方向**摆放，玩家能直接看出每个颜色正在往哪边走；
  工具栏图标同样按「光从左来」画成上红/右绿/下蓝。
- 工具栏数字键从 `1`–`5` 收窄为 `1`–`3`（可放置元件变成三种）。

### 已知取舍

- **目标点亮仍是超集判定**（`incoming & require === require`）：白光含三种颜色，所以打在
  单色目标上也算点亮。这条规则不会出现「多打一束杂光反而把已点亮的目标打灭」的反直觉情形；
  代价是颜色章之外棱镜并非必需元件 —— 它的定位是「一个元件把光分三路」的省元件做法。
- **第 2 章 `e02` / `e06` 保持原样**（肖工确认）：这两关本来就能用两束光把绿与蓝分别送进
  同一个目标，换成棱镜后依然可解，`par` 也没变。
- **安卓包随本次改版重新发布**：`versionName` 提到 `1.2.0`、`versionCode` 由 `1` 提到 `2`。
  不重打包的后果是实打实的：旧 1.1.0 包内没有棱镜，玩家会看到「工具栏少一个元件」的错位；
  而且同 `versionCode` 的包会被部分安装器判定为「无更新」而拒绝覆盖安装 ——
  这是发布后收到的真实反馈（「重启之后无法更新」）。

### 测试

- 单元测试 117 条、浏览器验收 62 条（桌面 + Pixel 7 两个视口）全部通过。
- `npm run verify:par` 确认 16 关的 `par` 与穷举出的最小元件数逐一相等。
- 安卓结构断言新增两条守卫：`versionName` 必须与 `package.json` 同线且 `versionCode` 递增；
  固定 debug 签名（`android/app/debug.keystore`）必须存在且被 gradle 显式引用。

## [1.1.0] - 2026-09-19

安卓版发布：同一份 Web 代码装进 WebView 外壳，可以直接安装到手机与平板。
Web 版没有任何行为变化 —— 唯一改动的桥探测只在原生桥存在时生效。

### Added

- **安卓 APK**：`light-game-android-1.1.0-debug.apk`，2.24 MB，包名 `com.xiaoxuhui.light`，
  最低支持 Android 7.0（API 24），**不申请任何权限**。
- **原生导出桥**：安卓端「导出」不再走 `Blob` + `<a download>`（WebView 不支持），
  改由 `LightAndroid.saveFile()` 交给原生写系统下载目录，提示语相应改为「已保存到…」。
  改动只落在 `storage.downloadFile()` 这一个出口内，引擎与渲染层不受影响。
- **打包工程**：`android/` 放 WebView 外壳工程，`npm run sync:android` 把 Web 资源同步进
  `assets/`，`npm run check:android` 只校验一致性；GitHub Actions 云端出包，
  本地不需要安装 Android SDK 或 JDK。
- **程序化应用图标**：由 `android/tools/make-icons.py` 用 Pillow 按 `renderer.js` 的光束配色
  绘制（白光射入中心散成红绿蓝三束），风格可控且零积分可复现，不依赖图像生成。

### Changed

- 版本号从 `1.0.0` 提到 `1.1.0`，网页版与安卓版共用同一条版本线。

### 已知取舍

- **debug 签名**：APK 用 debug 签名发布，便于直接安装；首次安装需允许「安装未知来源应用」。
- **存档不互通**：安卓端页面运行在 `appassets.androidplatform.net` 的 https 域下，
  `localStorage` 的 origin 与 `file://`、网页版都不同，安卓端存档从零开始。
- **无物理键盘**：手机端不提供键盘操作，但所有操作都有触摸等价路径（工具栏点击 + 网格点击）。

### 测试

- 单元测试 110 条（新增 10 条安卓外壳结构断言：包名、版本、桥名、同步清单、资源 XML 合法性），
  浏览器验收 60 条（桌面与移动两个视口）全部通过。
- 新增两条 CI 守卫：`res/**/*.xml` 的注释里不得出现连续两个减号（XML 规范禁止，
  aapt 会直接拒绝）；`index.html` 引用的资源必须都在同步清单内。
- 真机验证：装机确认 APK 可正常安装与运行。

### Fixed

- `colors.xml` 的注释里写了 CSS 变量名 `--bg-deep`，连续两个减号违反 XML 规范，
  导致 `:app:mergeDebugResources` 失败。改注释措辞并由上述守卫兜住。
- 安卓资源同步脚本原本要显式列举文件，但 CI 上 `assets/` 不存在时一致性断言会误报。
  改为「目录缺失则跳过」并在 CI 里先同步一次。

## [1.0.0] - 2026-09-19

Web 版首个正式版本：从一个空骨架变成 16 关完整可玩的游戏。

### Added

- **光路玩法**：8 方向传播、RGB 三色掩码合成与拆分。反射镜、分光镜、三色二向色镜六类元件，
  颜色拆分由掩码按位与完成，复合色光打上二向色镜会自然分成反射束与透射束。
- **16 个关卡**，分三章：第 0 章教学 4 关（依次引入直射、反射镜、分光镜、二向色镜），
  第 1 章「颜色」6 关，第 2 章「精算」6 关。关卡按前一关通关解锁。
- **星级评价**：通关 1 星，元件数 ≤ `par + 2` 得 2 星，≤ `par` 得 3 星。
  每关的 `par` 都经过验证：参考解能通关、元件数正好等于 `par`、去掉任意一个元件即无法通关。
- **撤销 / 重做**：快照式历史栈，上限 100 步，产生新操作时砍掉原有重做分支。
  快捷键 `Ctrl+Z` / `Ctrl+Shift+Z` / `Ctrl+Y`。
- **存档**：每关的布局自动存档，历史最好星数只增不减。`localStorage` 不可用时
  （隐私模式、`file://` 受限）自动降级为内存存档，本次会话内仍可正常游玩。
- **工作台快照**：把当前布局存为具名快照，上限 20 条，跨关卡共享。
- **JSON 导入导出**：导出当前关卡（关卡定义 + 玩家布局）为文件，可再导入。
  导入走完整校验，任一项不通过即整体拒绝并逐条列出原因，不做部分导入。
- **常驻通关反馈**：顶栏「下一关」按钮与舞台下方状态行始终在位，
  不再依赖结算浮层，避免「已通关但找不到路走」。
- **清空二次确认**：3 秒内再点一次才生效，防止移动端误触清光布局。
- **键盘全覆盖**：数字键选元件、方向键移动光标、`Enter` 放置、`R` 旋转、
  `Delete` / `Backspace` 删除、`Esc` 三态（关浮层 / 取消选中 / 开关卡列表）。
- **触摸支持**：全部交互走 Pointer Events，触摸端有独立的「橡皮」工具，不依赖右键与悬停。
- **响应式布局**：360×640 到 1280×800 全覆盖，含刘海安全区，工具栏与网格不重叠。
- **光路过载降级**：单次求解超过 4096 光段时停止追踪并给出警示，不冻结页面、不影响切关。
- **调试手柄** `window.__lightGame`，供 e2e 与控制台使用。

### Changed

- 入口页新增 `scripts/storage.js`、`scripts/light-core.js`、`scripts/light-engine.js`、
  `scripts/levels.js`、`scripts/renderer.js`，按 `defer` 顺序加载，共享全局命名空间。
- 骨架阶段的待机光晕占位渲染被真实场景渲染整体替换，没有在原函数上叠加功能。

### Fixed

- 教学第 1 关进关即通关时不弹结算浮层，且「下一关」入口随浮层一起消失，
  玩家会卡在已通关的关卡上。改为常驻入口 + 常驻状态行。
- `result.overflow` 在引擎里会置位，但渲染层与编排层都没有消费它，
  光路被截断时玩家毫无提示。已接到状态行。

### 测试

- 单元测试 95 条：反射规则、分光与二向色、颜色掩码、求解终止与剪枝、幂等、星级、
  16 关自检（含参考解与 `par` 最小性）、存档往返、导入拒绝路径、结构断言。
- 浏览器验收 60 条：桌面与移动两个视口，覆盖离线可玩、键盘与指针全流程、
  撤销重做、存档、导入导出、过载降级、三档响应式布局、减少动效。
- CI：Node 20.19 / 22.13 / 24 矩阵跑语法检查 + ESLint + 单测，另有 Chromium 跑 Playwright。

## [0.1.0] - 2026-09-19

工程骨架建立，尚无可玩玩法。

### Added

- 项目脚手架：单页入口 `index.html`、`styles/main.css`、`scripts/app.js`，纯静态、零运行时依赖，
  可 `file://` 直接打开，也可通过 `npm run serve` 在 127.0.0.1:4174 访问。
- 应用引导层：按 `devicePixelRatio` 初始化画布（封顶 2 倍）、`ResizeObserver` 跟随窗口变化、
  页面隐藏时暂停渲染循环、响应 `prefers-reduced-motion`。
- 待机光晕占位渲染，用于验证画布与渲染链路，玩法定稿后由真实场景替换。
- 工程门禁：ESLint 扁平配置、`node --test` 结构断言、Playwright 浏览器冒烟（桌面 + 移动视口）、
  GitHub Actions CI（Node 20.19 / 22.13 / 24 矩阵 + 浏览器冒烟）。
- 许可证与协作文件：MIT License、贡献指南、行为准则、安全策略、`doc/` 三份规划文档。
- `.gitattributes` 换行规范，为后续安卓构建脚本（`gradlew` 必须 LF）预留。
